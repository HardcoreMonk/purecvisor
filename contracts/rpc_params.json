{
  "device.nic.list":        { "required": ["vm_id"], "optional": [] },
  "device.nic.attach":      { "required": ["vm_id"], "optional": ["bridge", "model"] },
  "device.nic.detach":      { "required": ["vm_id", "mac"], "optional": [] },
  "vm.eject":               { "required": ["vm_id"], "optional": [] },
  "container.nic.detach":   { "required": ["name", "nic_name"], "optional": [] },
  "container.nic.attach":   { "required": ["name"], "optional": ["bridge", "hwaddr"] },
  "container.snapshot.create": { "required": ["name", "snap_name"], "optional": [] },
  "container.snapshot.delete": { "required": ["name", "snap_name"], "optional": [] },
  "network.mode_set":       { "required": ["name", "mode", "cidr"], "optional": [] },
  "network.dhcp_toggle":    { "required": ["bridge"], "optional": ["enable"] },
  "network.bind_phys":      { "required": ["bridge", "iface"], "optional": [] },
  "network.qos.set":        { "required": [["interface", "vm_name"]], "optional": ["rate_mbps", "burst_kb", "direction"], "keys_via": "alias-fallback" },
  "network.qos.get":        { "required": ["interface"], "optional": [] },
  "network.qos.remove":     { "required": ["interface"], "optional": ["direction"] },
  "ovn.acl.add":            { "required": ["switch", "match"], "optional": ["direction", "priority", "action"] },
  "ovn.acl.list":           { "required": [], "optional": ["switch"] },
  "ovn.dhcp.enable":        { "required": ["subnet", "gateway"], "optional": ["switch"] },
  "vm.security_group.set":  { "required": ["vm", "security_group"], "optional": [] },
  "container.set_bandwidth":{ "required": ["name"], "optional": ["nic_name", "inbound_kbps", "outbound_kbps"] },
  "container.set_limits":   { "required": ["name"], "optional": ["cpu_percent", "cpu_weight", "memory_mb"] },
  "storage.pool.health":    { "required": [], "optional": ["pool"] },
  "node.drain":             { "required": [], "optional": ["timeout_sec"] },
  "container.health.set":   { "required": ["name", "type", "target"], "optional": ["interval_sec"] },
  "device.disk.attach":     { "required": ["vm_id", "source", "target"], "optional": ["bus"] },
  "auth.totp.status":              { "required": [], "optional": ["username"] },
  "auth.totp.disable":             { "required": ["code"], "optional": ["username"] },
  "auth.totp.reset":               { "required": ["username"], "optional": [] },
  "auth.totp.recovery.regenerate": { "required": ["code"], "optional": ["username"] },
  "vpc.list":               { "required": [], "optional": ["tenant"] },
  "vpc.get":                { "required": ["vpc_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.subnet.list":        { "required": ["vpc_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.attachment.list":    { "required": ["vpc_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.service.list":       { "required": ["vpc_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.status":             { "required": [], "optional": ["tenant"] },
  "vpc.create":             { "required": ["name", "egress_mode"], "optional": ["tenant", "backend", "subnet_name", "subnet_cidr", "subnet_mtu"], "keys_via": "handler-vpc-helper" },
  "vpc.delete":             { "required": ["vpc_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.egress.set":         { "required": ["vpc_id", "egress_mode", "expected_revision"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.subnet.create":      { "required": ["vpc_id", "name", "cidr", "expected_revision"], "optional": ["tenant", "mtu"], "keys_via": "handler-vpc-helper" },
  "vpc.subnet.delete":      { "required": ["subnet_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.attachment.create":  { "required": ["subnet_id", "vm"], "optional": ["tenant", "ip_address"], "keys_via": "handler-vpc-helper" },
  "vpc.attachment.delete":  { "required": ["attachment_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.service.publish":    { "required": ["attachment_id", "protocol", "listen_address", "listen_port", "target_port", "allowed_sources"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.service.unpublish":  { "required": ["publish_id"], "optional": ["tenant"], "keys_via": "handler-vpc-helper" },
  "vpc.reconcile":          { "required": [], "optional": [] }
}
