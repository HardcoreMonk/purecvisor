# check_fe_rpc_params 역방향 위반 착지 baseline (2026-08-07 P2) — 단조감소 래칫.
#
# 역방향 위반 = 백엔드 핸들러가 **필수로 요구하는** 키(has_member 부정 가드 + 에러 리턴으로
# 식별)를 UI 가 요청 본문에 싣지 않는 것. 그대로 두면 화면 기능이 -32602 로 조용히 실패한다.
#
# 형식: <파일>|<RPC 메서드>|<UI 가 안 보내는 필수 키를 쉼표로>
# 이 명단 밖의 신규 위반만 FAIL. 시정하면 이 명단에서 제거한다(추가는 부채 증가다).
#
# 현재 미해결 역방향 위반은 없다. 새 필수 키 누락은 이 파일에 추가하지 않고 호출부에서
# 즉시 시정한다.
